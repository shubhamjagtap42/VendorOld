import { AddnewrowDirective } from './addnewrow.directive';

describe('AddnewrowDirective', () => {
  it('should create an instance', () => {
    const directive = new AddnewrowDirective();
    expect(directive).toBeTruthy();
  });
});
